# Milestone 0 acceptance

1. Copy `.env.example` to `.env.local` and provide valid Supabase values.
2. Start the app and visit `/projects`.
3. Confirm the professional app shell renders and the Project route is available.
4. Confirm `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass.
